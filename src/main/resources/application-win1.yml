spring:
  application:
    name: syncfile

logging:
  config: classpath:logback-spring.xml

file:
  interval: 1000
  suffix: .java,.xml,.md,.zip,.yaml,.yml,.txt,.kt
  source:
    path: D:/sync/

  target:
    path: V:/jinfeng/sync/
